# Configuración del servidor
PORT=3000

# Base de datos MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=comesur_db

# JWT
JWT_SECRET=cambia_esto_por_una_clave_segura_y_larga

# Google OAuth (obtener en console.cloud.google.com)
GOOGLE_CLIENT_ID=tu_google_client_id_aqui.apps.googleusercontent.com

# Facebook OAuth (obtener en developers.facebook.com)
# Solo se necesita en el backend para verificar tokens via Graph API
# El App ID se configura también en la app Flutter
